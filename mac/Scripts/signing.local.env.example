# 本地代码签名身份配置（模板）
#
# 用法：
#   cp signing.local.env.example signing.local.env
# 然后填入本机的签名身份。signing.local.env 已被 .gitignore 忽略，不会进仓库。
#
# 查看可用身份：
#   security find-identity -v -p codesigning
# 输出形如：
#   1) A1B2C3...  "Apple Development: 你的名字 (TEAMID1234)"
# 取行首那串 40 位 SHA-1 哈希填到下面。
#
# 为什么用哈希而不是证书名：同名证书可能有多张（例如已吊销的旧证书），
# 按名字签会随机撞上它，报 CSSMERR_TP_CERT_REVOKED。
#
# 留空或设成 "-" 表示 ad-hoc 签名 —— 此时每次重新编译后首次读钥匙串都会弹授权框。
CODESIGN_IDENTITY=
