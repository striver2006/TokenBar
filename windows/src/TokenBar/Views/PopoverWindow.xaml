<Window x:Class="TokenBar.Views.PopoverWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="TokenBar" Height="450" Width="320"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ResizeMode="NoResize">
    <Border Background="#F9FAFB" CornerRadius="10" BorderBrush="#E5E7EB" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <Border Grid.Row="0" Background="#FFFFFF" Padding="14,10" CornerRadius="10,10,0,0" BorderBrush="#E5E7EB" BorderThickness="0,0,0,1">
                <DockPanel>
                    <StackPanel Orientation="Vertical">
                        <TextBlock Text="TokenBar" FontWeight="Bold" FontSize="14" Foreground="#111827"/>
                        <TextBlock x:Name="TxtSubtitle" Text="模型额度监控" FontSize="10" Foreground="#6B7280"/>
                    </StackPanel>
                    <Button x:Name="BtnRefresh" Content="刷新" HorizontalAlignment="Right" Padding="8,3" Background="#F3F4F6" BorderBrush="#D1D5DB" Click="BtnRefresh_Click"/>
                </DockPanel>
            </Border>

            <!-- Body: List of Provider Cards -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="PnlCards" Margin="12" />
            </ScrollViewer>

            <!-- Footer -->
            <Border Grid.Row="2" Background="#F3F4F6" Padding="12,8" CornerRadius="0,0,10,10" BorderBrush="#E5E7EB" BorderThickness="0,1,0,0">
                <DockPanel>
                    <TextBlock x:Name="TxtFooter" Text="准备就绪" FontSize="10" Foreground="#6B7280" VerticalAlignment="Center"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Content="⚙" Width="24" Height="24" Margin="0,0,6,0" Click="BtnSettings_Click" ToolTip="Settings"/>
                        <Button Content="✕" Width="24" Height="24" Click="BtnClose_Click" ToolTip="Close"/>
                    </StackPanel>
                </DockPanel>
            </Border>
        </Grid>
    </Border>
</Window>
